# Ads Library 프로젝트 구조 정리

## 📌 프로젝트 개요
Meta(Facebook/Instagram) 광고 라이브러리를 스크래핑하고 관리하는 풀스택 웹 애플리케이션

---

## 🎨 Frontend (React)

---

## 🏗️ 앱 구조 (App Structure)

### [App.js](src/App.js) (애플리케이션 진입점)
- **위치**: `src/App.js`
- **주요 기능**:
  - ChakraProvider로 테마 적용
  - AuthProvider로 인증 컨텍스트 제공
  - 최상위 라우팅 설정
- **라우트 구조**:
  - `/auth/*` → AuthLayout (로그인/회원가입)
  - `/admin/*` → AdminLayout (메인 대시보드)
  - `/rtl/*` → RTLLayout (RTL 레이아웃)
  - `/` → `/admin`으로 리다이렉트

---

### [routes.js](src/routes.js) (라우트 설정)
- **위치**: `src/routes.js`
- **주요 기능**:
  - 모든 페이지의 메뉴 구조 정의
  - 사이드바에 표시될 메뉴 항목들
  - 각 페이지의 아이콘, 경로, 컴포넌트 매핑
- **현재 활성화된 메뉴**:
  1. **Main Home** (`/admin/monitoring`) - MdHome 아이콘
  2. **Ads-Library** (`/admin/ads-library`) - MdAdsClick 아이콘 ⭐ 메인 기능
  3. **NFT Marketplace** (`/admin/nft-marketplace`) - MdOutlineShoppingCart 아이콘
  4. **Data Tables** (`/admin/data-tables`) - MdBarChart 아이콘
  5. **Profile** (`/admin/profile`) - MdPerson 아이콘
- **비활성화된 메뉴**:
  - Main Dashboard (주석 처리됨)
- **연관 파일**:
  - `Sidebar.js` - 이 routes 데이터를 받아서 메뉴 렌더링
  - `AdminLayout` - 라우팅 처리

---

## 🖼️ 레이아웃 (Layouts)

### [AdminLayout (index.js)](src/layouts/admin/index.js)
- **위치**: `src/layouts/admin/index.js`
- **주요 기능**:
  - 관리자 페이지의 전체 레이아웃 구조
  - 사이드바 + 네비게이션바 + 콘텐츠 영역
  - routes.js의 라우트를 실제 페이지로 렌더링
- **레이아웃 구성**:
  - **Sidebar** (고정, 왼쪽, 290px 너비)
  - **Navbar** (상단, Portal로 렌더링)
  - **Content Area** (메인 콘텐츠)
  - **Footer** (하단)
- **반응형 처리**:
  - xl 이상: 사이드바 고정, 콘텐츠 영역 `calc(100% - 290px)`
  - xl 미만: 사이드바 숨김, 햄버거 메뉴로 드로어 형태
- **핵심 함수**:
  - `getActiveRoute(routes)`: 현재 활성화된 페이지 이름 (Navbar에 표시)
  - `getRoutes(routes)`: routes.js의 설정을 실제 React Router Route로 변환
- **연관 파일**:
  - `routes.js` - 라우트 설정
  - `Sidebar.js` - 사이드바 렌더링
  - `NavbarAdmin.js` - 네비게이션바

---

## 🧭 사이드바 (Sidebar)

### [Sidebar.js](src/components/sidebar/Sidebar.js)
- **위치**: `src/components/sidebar/Sidebar.js`
- **주요 기능**:
  - 왼쪽 고정 사이드바 렌더링
  - routes.js의 메뉴 항목들을 표시
  - 반응형 처리 (데스크톱 고정 / 모바일 드로어)
- **사이드바 설정**:
  - 너비: `230px`
  - 높이: `100vh`
  - 위치: 고정 (`position: fixed`)
  - 배경: 라이트/다크 모드 지원
- **두 가지 모드**:
  1. **Sidebar** (xl 이상): 고정 사이드바
  2. **SidebarResponsive** (xl 미만): 햄버거 메뉴 → 드로어
- **연관 컴포넌트**:
  - `Content.js` - 사이드바 내부 콘텐츠
  - `routes.js` - 메뉴 데이터

---

### [Content.js](src/components/sidebar/components/Content.js)
- **위치**: `src/components/sidebar/components/Content.js`
- **주요 기능**:
  - 사이드바 내부의 실제 콘텐츠 렌더링
  - 브랜드 로고 + 메뉴 링크들
- **구성**:
  - `Brand` - 상단 로고/브랜드명
  - `Links` - 메뉴 링크 목록
- **연관 컴포넌트**:
  - `Brand.js` - 브랜드 로고
  - `Links.js` - 메뉴 링크 렌더링

---

### [Brand.js](src/components/sidebar/components/Brand.js)
- **위치**: `src/components/sidebar/components/Brand.js`
- **주요 기능**:
  - 사이드바 상단의 로고/브랜드명 표시
  - 홈으로 이동하는 링크

### [Links.js](src/components/sidebar/components/Links.js)
- **위치**: `src/components/sidebar/components/Links.js`
- **주요 기능**:
  - routes.js의 메뉴 항목들을 NavLink로 렌더링
  - 현재 활성화된 메뉴 하이라이트
  - 아이콘 + 텍스트 형태로 표시

---

## 🧭 네비게이션바 (Navbar)

### [NavbarAdmin.js](src/components/navbar/NavbarAdmin.js)
- **위치**: `src/components/navbar/NavbarAdmin.js`
- **주요 기능**:
  - 상단 네비게이션바 렌더링
  - 현재 페이지 이름 표시 (breadcrumb)
  - 검색, 알림, 프로필 등 링크
- **스크롤 감지**:
  - 스크롤 시 스타일 변경 (배경, 그림자)
- **연관 컴포넌트**:
  - `NavbarLinksAdmin.js` - 네비바 오른쪽 링크들

### [NavbarLinksAdmin.js](src/components/navbar/NavbarLinksAdmin.js)
- **위치**: `src/components/navbar/NavbarLinksAdmin.js`
- **주요 기능**:
  - 네비바 오른쪽의 아이콘 링크들
  - 검색, 알림, 다크모드 토글, 프로필 메뉴

---

## 📄 페이지 (Pages/Views)

#### [index.jsx](src/views/admin/metaArchive/index.jsx) (MetaArchive 메인 페이지)
- **위치**: `src/views/admin/metaArchive/index.jsx`
- **주요 기능**:
  - Meta 광고 아카이브 메인 페이지
  - 광고 검색 및 스크래핑 기능 제공
  - 페이지네이션으로 광고 목록 표시
  - 반응형 레이아웃 (모바일/데스크톱)
- **포함된 기능**:
  - 광고 데이터 로딩 및 필터링
  - 검색 히스토리 관리
  - Job 트래킹 (스크래핑 진행 상황)
  - 광고 상세 모달 표시
  - 페이지네이션 (이전/다음)
- **연관 컴포넌트**:
  - `SearchBar` - 검색 입력
  - `SavedSearchesSidebar` - 검색 히스토리
  - `AdGrid` - 광고 그리드 표시
  - `JobTracker` - 스크래핑 진행 상황
  - `AdDetailModal` - 광고 상세 정보
- **API 연동**:
  - `getAds()` - 광고 목록 조회

---

### 🧩 컴포넌트 (Components)

#### [SearchBar.jsx](src/views/admin/metaArchive/components/SearchBar.jsx)
- **위치**: `src/views/admin/metaArchive/components/SearchBar.jsx`
- **사용 페이지**: MetaArchive 메인 페이지
- **주요 기능**:
  - 광고 검색 및 스크래핑 시작
  - 검색 타입 선택 (Keyword / Advertiser)
  - 플랫폼 선택 (Meta, Google, Naver, Kakao)
  - 검색어 입력 폼
- **핵심 동작**:
  - 검색 실행 시 백엔드에 스크래핑 Job 생성 요청
  - Job ID 반환받아 부모 컴포넌트에 전달 (`onScrapeStart`)
  - 검색 히스토리에 자동 저장 (`onSearchSaved` 콜백으로 사이드바 새로고침)
- **연관 API**:
  - `scrapeByKeyword()` - 키워드 검색
  - `scrapeByAdvertiser()` - 광고주 검색
- **연관 컴포넌트**:
  - `SavedSearchesSidebar` - 검색 후 히스토리 업데이트 트리거
  - `JobTracker` - 생성된 Job ID 전달

---

#### [SavedSearchesSidebar.jsx](src/views/admin/metaArchive/components/SavedSearchesSidebar.jsx)
- **위치**: `src/views/admin/metaArchive/components/SavedSearchesSidebar.jsx`
- **사용 페이지**: MetaArchive 메인 페이지
- **주요 기능**:
  - 사용자의 검색 히스토리 표시
  - 저장된 검색어 클릭 시 해당 검색 결과 재조회
  - 검색 새로고침 (최신 광고 재수집)
  - 반응형 레이아웃 (고정 사이드바 / 인라인 카드)
- **핵심 동작**:
  - `refreshTrigger` prop이 변경되면 자동으로 검색 히스토리 다시 로드
  - 각 검색 항목의 광고 개수 표시
  - 새로고침 버튼으로 해당 검색어 재스크래핑 가능
- **표시 모드**:
  - **Fixed 모드** (xl 이상): 왼쪽에 고정된 사이드바
  - **Inline 모드** (xl 미만): SearchBar 아래 카드 형태로 표시
- **연관 API**:
  - `getSearchHistory()` - 검색 히스토리 조회
  - `scrapeByKeyword()` / `scrapeByAdvertiser()` - 재스크래핑
- **연관 컴포넌트**:
  - `SearchBar` - 새 검색 시 히스토리 업데이트
  - `index.jsx` - 클릭 시 필터 적용

---

#### [AdGrid.jsx](src/views/admin/metaArchive/components/AdGrid.jsx)
- **위치**: `src/views/admin/metaArchive/components/AdGrid.jsx`
- **사용 페이지**: MetaArchive 메인 페이지
- **주요 기능**:
  - 광고 목록을 그리드 형태로 표시
  - 각 광고의 썸네일, 광고주명, 텍스트, 플랫폼 아이콘 표시
  - 1:1 비율에 가장 가까운 이미지를 썸네일로 선택
  - 광고 클릭 시 상세 모달 열기
- **핵심 동작**:
  - `selectBestMedia()` 함수로 정사각형에 가까운 이미지 우선 선택
  - 플랫폼 아이콘 표시 (Facebook, Instagram, Messenger, Threads, Audience Network)
  - 반응형 그리드 (모바일 1열, 태블릿 3열, 데스크톱 4-5열)
- **연관 컴포넌트**:
  - `AdDetailModal` - 클릭 시 상세 정보 표시
  - `index.jsx` - 광고 데이터 전달받음

---

#### [JobTracker.jsx](src/views/admin/metaArchive/components/JobTracker.jsx)
- **위치**: `src/views/admin/metaArchive/components/JobTracker.jsx`
- **사용 페이지**: MetaArchive 메인 페이지
- **주요 기능**:
  - 스크래핑 Job의 실시간 진행 상황 표시
  - 2초마다 Job 상태 폴링
  - 진행률 프로그레스 바
  - 수집된 광고 개수 표시
- **상태 표시**:
  - `pending`: 대기 중
  - `processing`: 스크래핑 중
  - `completed`: 완료
  - `failed`: 실패
- **핵심 동작**:
  - Job 완료 시 `onComplete` 콜백 호출하여 광고 목록 새로고침
  - 실패 시 에러 메시지 표시
  - 자동으로 폴링 중단 (완료/실패 시)
- **연관 API**:
  - `getJobStatus(jobId)` - Job 상태 조회
- **연관 컴포넌트**:
  - `SearchBar` - Job ID 전달받음
  - `index.jsx` - 완료 시 광고 목록 갱신

---

#### [AdDetailModal.jsx](src/views/admin/metaArchive/components/AdDetailModal.jsx)
- **위치**: `src/views/admin/metaArchive/components/AdDetailModal.jsx`
- **사용 페이지**: MetaArchive 메인 페이지
- **주요 기능**:
  - 광고의 상세 정보를 모달로 표시
  - 모든 미디어 (이미지/동영상) 나란히 표시
  - OCR 추출 텍스트 표시
  - 광고 통계, 링크, 날짜 정보 표시
- **표시 정보**:
  - 미디어: 정사각형 → 세로형 → 가로형 순으로 정렬하여 표시
  - OCR 텍스트: 각 이미지에서 추출된 텍스트
  - 광고 텍스트: 본문, 링크 제목, 링크 설명
  - 통계: Ad Archive ID, 플랫폼, 검색 타입, 노출 기간, 노출수, 광고비
  - 링크: 페이지 URL, 랜딩 URL
- **연관 컴포넌트**:
  - `AdGrid` - 광고 클릭 시 모달 오픈
  - `index.jsx` - 모달 상태 관리

---

### 📊 기타 페이지 (Other Pages)

#### [Monitoring (index.js)](src/views/admin/monitoring/index.js)
- **위치**: `src/views/admin/monitoring/index.js`
- **접근 경로**: `/admin/monitoring`
- **주요 기능**:
  - 경쟁사 광고 모니터링 대시보드
  - 카테고리별 경쟁사 필터링 및 조회
  - 경쟁사별 광고 트렌드 분석
- **카테고리 목록**:
  - 전체
  - 뷰티/화장품
  - 패션/의류
  - 식품/음료
  - IT/전자
  - 여행/숙박
- **경쟁사 데이터** (샘플 데이터):
  - 메디큐브 - 뷰티/화장품, 총 12개 광고
  - 올리브영 - 뷰티/화장품, 총 8개 광고
  - 토리든 - 뷰티/화장품, 총 15개 광고
- **UI 구성**:
  - 카테고리 필터 버튼
  - 경쟁사 카드 그리드 (광고 수, 최근 활동, 상세보기 버튼)
  - 반응형 그리드 레이아웃
- **연관 파일**:
  - `routes.js` - 라우트 설정

---

#### [NFT Marketplace (index.jsx)](src/views/admin/marketplace/index.jsx)
- **위치**: `src/views/admin/marketplace/index.jsx`
- **접근 경로**: `/admin/nft-marketplace`
- **주요 기능**:
  - NFT 마켓플레이스 데모 페이지 (Horizon UI 템플릿 샘플)
  - NFT 컬렉션 표시
  - NFT 상세 정보 및 가격 표시
- **UI 구성**:
  - 인기 NFT 배너 (Swiper Carousel)
  - NFT 카드 그리드
  - 작가 정보 섹션
  - 히스토리 테이블
- **주요 컴포넌트**:
  - `Banner` - 상단 NFT 배너 슬라이더
  - `HistoryItem` - 거래 히스토리 항목
  - `NFT` - NFT 카드
  - `TopCreatorTable` - 인기 작가 테이블
- **데이터**:
  - 샘플 NFT 이미지 및 정보
  - 가격 데이터 (ETH)
  - 작가 정보
- **연관 파일**:
  - `routes.js` - 라우트 설정
  - NFT 이미지 파일들 (assets/img/nfts/)

---

#### [Data Tables (index.jsx)](src/views/admin/dataTables/index.jsx)
- **위치**: `src/views/admin/dataTables/index.jsx`
- **접근 경로**: `/admin/data-tables`
- **주요 기능**:
  - 다양한 형태의 데이터 테이블 데모
  - 4가지 테이블 타입 showcase
  - 반응형 테이블 디자인
- **테이블 종류**:
  1. **DevelopmentTable** - 개발 프로젝트 진행 상황 테이블
  2. **CheckTable** - 체크박스가 포함된 선택 가능 테이블
  3. **ColumnsTable** - 컬럼 정렬 및 필터링 테이블
  4. **ComplexTable** - 복잡한 데이터 구조 테이블
- **공통 기능**:
  - 정렬 (sorting)
  - 페이지네이션
  - 검색/필터링
  - 반응형 디자인
- **샘플 데이터**:
  - tableDataDevelopment
  - tableDataCheck
  - tableDataColumns
  - tableDataComplex
- **연관 파일**:
  - `routes.js` - 라우트 설정
  - `variables/tableData*` - 샘플 데이터

---

#### [Profile (index.jsx)](src/views/admin/profile/index.jsx)
- **위치**: `src/views/admin/profile/index.jsx`
- **접근 경로**: `/admin/profile`
- **주요 기능**:
  - 사용자 프로필 페이지
  - 사용자 정보 및 활동 내역 표시
  - 스토리지 사용량 확인
- **UI 구성**:
  - **Banner** - 프로필 배너 및 아바타
  - **General** - 일반 정보 섹션
  - **Storage** - 스토리지 사용량 (용량별 차트)
  - **Projects** - 사용자 프로젝트 목록
  - **Notifications** - 알림 설정
- **주요 컴포넌트**:
  - `Banner` - 프로필 헤더
  - `General` - 개인정보 표시
  - `Storage` - 스토리지 통계
  - `Project` - 프로젝트 카드
  - `Notifications` - 알림 설정 토글
- **표시 정보**:
  - 이름, 이메일, 역할
  - 프로필 사진
  - 소셜 미디어 링크
  - 스토리지 사용 통계
- **연관 파일**:
  - `routes.js` - 라우트 설정
  - 프로필 이미지 파일들

---

### 🔧 서비스 (Services)

#### [apiService.js](src/services/apiService.js) (Frontend API)
- **위치**: `src/services/apiService.js`
- **주요 기능**:
  - 백엔드 API 호출을 위한 중앙 집중식 서비스
  - Axios 인터셉터로 자동 인증 토큰 주입
  - 스크래핑 API와 읽기 API 분리
- **스크래핑 API** (백엔드 필수):
  - `scrapeByKeyword(keyword, platform)` - 키워드로 스크래핑 시작
  - `scrapeByAdvertiser(advertiser, platform)` - 광고주로 스크래핑 시작
  - `getJobStatus(jobId)` - Job 상태 조회
- **읽기 API** (Supabase 직접 호출):
  - `getAds(filters)` - 광고 목록 조회 (필터링, 페이지네이션)
  - `getSearchHistory()` - 검색 히스토리 조회
- **연관 파일**:
  - `supabaseService.js` - 실제 Supabase 쿼리 구현
  - 모든 프론트엔드 컴포넌트에서 사용

---

#### [supabaseService.js](src/services/supabaseService.js)
- **위치**: `src/services/supabaseService.js`
- **주요 기능**:
  - Supabase 직접 쿼리를 위한 서비스
  - 백엔드를 거치지 않고 DB에 직접 접근 (읽기 전용)
- **주요 함수**:
  - `getAds(filters)` - 광고 목록 조회 (검색어, 광고주, 플랫폼 필터링, 페이지네이션)
  - `getAdById(id)` - 광고 상세 조회 (미디어 포함)
  - `deleteAd(id)` - 광고 삭제
  - `getSearchHistory()` - 검색 히스토리 조회 및 광고 개수 집계
  - `getJobs(limit)` - Job 목록 조회
- **핵심 동작**:
  - 복잡한 Supabase 쿼리 (JOIN, 필터링, 정렬)
  - 검색 히스토리 그룹화 및 집계
  - 페이지네이션 처리
- **연관 파일**:
  - `apiService.js` - 이 서비스의 함수들을 호출
  - `supabase.js` (config) - Supabase 클라이언트

---

### 🔐 컨텍스트 (Contexts)

#### [AuthContext.js](src/contexts/AuthContext.js)
- **위치**: `src/contexts/AuthContext.js`
- **주요 기능**:
  - 전역 인증 상태 관리 (React Context)
  - 사용자 로그인, 회원가입, 로그아웃 처리
  - Supabase Auth 연동
- **제공하는 값**:
  - `user` - 현재 로그인한 사용자 정보
  - `loading` - 인증 로딩 상태
  - `signIn(email, password)` - 로그인
  - `signUp(email, password)` - 회원가입
  - `signOut()` - 로그아웃
- **핵심 동작**:
  - 페이지 로드 시 세션 자동 복구
  - Auth 상태 변경 감지 (onAuthStateChange)
  - 앱 전체에 사용자 정보 제공
- **사용 방법**:
  - `App.js`에서 `<AuthProvider>` 래핑
  - 컴포넌트에서 `useAuth()` 훅으로 접근
- **연관 파일**:
  - `App.js` - AuthProvider로 앱 래핑
  - `supabase.js` - Supabase 클라이언트

---

#### [SidebarContext.js](src/contexts/SidebarContext.js)
- **위치**: `src/contexts/SidebarContext.js`
- **주요 기능**:
  - 사이드바 토글 상태 관리
  - 모바일에서 사이드바 열기/닫기
- **제공하는 값**:
  - `toggleSidebar` - 사이드바 열림/닫힘 상태
  - `setToggleSidebar` - 상태 변경 함수
- **연관 파일**:
  - `AdminLayout` - SidebarContext Provider
  - `Sidebar.js` - 토글 상태 사용

---

### ⚙️ 설정 (Config)

#### [supabase.js](src/config/supabase.js) (Frontend)
- **위치**: `src/config/supabase.js`
- **주요 기능**:
  - Supabase 클라이언트 초기화
  - 환경변수에서 URL과 키 로드
- **환경변수**:
  - `REACT_APP_SUPABASE_URL` - Supabase 프로젝트 URL
  - `REACT_APP_SUPABASE_ANON_KEY` - 공개(anon) 키
- **연관 파일**:
  - 모든 Supabase 사용 파일에서 import

---

## ⚙️ Backend (Node.js + Express)

### 🖥️ 서버 (Server)

#### [server.js](backend/src/server.js)
- **위치**: `backend/src/server.js`
- **주요 기능**:
  - Express 서버 초기화 및 실행
  - 미들웨어 설정 (CORS, Helmet, Morgan)
  - API 라우트 등록
  - 에러 핸들링
- **등록된 라우트**:
  - `/api/scrape` - 스크래핑 요청 처리
  - `/api/jobs` - Job 상태 조회
  - `/health` - 서버 헬스체크
- **연관 파일**:
  - `scrapeRoutes.js` - 스크래핑 라우트
  - `jobRoutes.js` - Job 라우트
  - `scrapeQueue.js` - Bull Queue 초기화

---

### 🛣️ 라우트 (Routes)

#### [scrapeRoutes.js](backend/src/routes/scrapeRoutes.js)
- **위치**: `backend/src/routes/scrapeRoutes.js`
- **주요 기능**:
  - 스크래핑 요청을 받아 Bull Queue에 Job 추가
  - 검색어 유효성 검증 (한글, 영문, 숫자, 특수문자)
  - 검색 히스토리 DB 저장
- **API 엔드포인트**:
  - `POST /api/scrape/keyword` - 키워드로 스크래핑
  - `POST /api/scrape/advertiser` - 광고주로 스크래핑
- **핵심 동작**:
  1. 검색어 유효성 검증 (`validateSearchQuery`)
  2. Bull Queue에 Job 추가 (`addScrapeJob`)
  3. DB에 Job 레코드 생성 (`createJob`)
  4. 검색 히스토리 저장 (Supabase `user_search_history`)
  5. Job ID 반환
- **연관 파일**:
  - `scrapeQueue.js` - Job 큐 관리
  - `dbService.js` - DB 작업
  - `auth.js` - 인증 미들웨어

---

#### [jobRoutes.js](backend/src/routes/jobRoutes.js)
- **위치**: `backend/src/routes/jobRoutes.js`
- **주요 기능**:
  - Job 상태 조회 API
  - 사용자별 Job 목록 조회
- **API 엔드포인트**:
  - `GET /api/jobs/:jobId` - 특정 Job 상태 조회
  - `GET /api/jobs` - 사용자의 Job 목록 조회
- **핵심 동작**:
  - Bull Queue와 DB에서 Job 정보 병합
  - 실시간 진행률 및 상태 반환
- **연관 파일**:
  - `scrapeQueue.js` - Bull Queue 접근
  - `auth.js` - 인증 미들웨어

---

### 🕷️ 스크래퍼 (Scrapers)

#### [metaAdLibrary.js](backend/src/scrapers/metaAdLibrary.js)
- **위치**: `backend/src/scrapers/metaAdLibrary.js`
- **주요 기능**:
  - Meta(Facebook) Ad Library 스크래핑 (Playwright 사용)
  - HTML + GraphQL 하이브리드 방식
  - Cloudinary 이미지/동영상 업로드 (선택적)
  - OCR 텍스트 추출 (선택적)
- **스크래핑 프로세스**:
  1. 브라우저 열기 (Playwright Chromium)
  2. Meta Ad Library 페이지 접근
  3. 페이지 새로고침 (GraphQL 응답 캡처를 위해 필수)
  4. HTML에서 초기 광고 추출
  5. 무한 스크롤로 GraphQL 응답 캡처 (자동 종료 감지)
  6. 광고 데이터 파싱 및 중복 제거
  7. Supabase에 광고 저장 (`ad_archives`, `ad_media`)
- **고급 기능**:
  - **자동 종료**: 5회 연속 새 데이터 없으면 스크롤 중단
  - **중복 방지**: `ad_archive_id` 기반 upsert
  - **Cloudinary 업로드**: 이미지/동영상을 Cloudinary에 업로드하여 안정적인 URL 확보
  - **OCR**: 이미지에서 텍스트 추출 (현재 비활성화, 속도 개선)
  - **재시도 로직**: 저장 실패 시 3회 재시도
- **반환 데이터**:
  - `ads`: 포맷팅된 광고 배열
  - `savedAds`: 저장된 광고 수 (신규 + 업데이트)
  - `newAds`: 신규 광고 수
  - `updatedAds`: 업데이트된 광고 수
  - `failedAds`: 실패한 광고 수
- **연관 파일**:
  - `cloudinaryService.js` - 미디어 업로드
  - `ocrService.js` - OCR 텍스트 추출
  - `supabase.js` - DB 저장

---

### 📬 큐 (Queues)

#### [scrapeQueue.js](backend/src/queues/scrapeQueue.js)
- **위치**: `backend/src/queues/scrapeQueue.js`
- **주요 기능**:
  - Bull Queue로 스크래핑 Job 비동기 처리
  - Redis 기반 Job 관리
  - Job 진행 상황 추적 및 DB 업데이트
- **Queue 설정**:
  - Redis 연결 (호스트, 포트, 비밀번호, TLS)
  - 재시도: 3회 (지수 백오프 60초)
  - 타임아웃: 15분 (Cloudinary 업로드 고려)
- **Job 처리 프로세스**:
  1. Queue에서 Job 가져오기
  2. Job 상태 'processing'으로 변경
  3. `scrapeMetaAds()` 호출 (Cloudinary 자동 업로드)
  4. 진행률 업데이트 (콜백)
  5. 결과 DB 저장 (신규/업데이트/실패 개수)
  6. Job 상태 'completed' 또는 'failed'로 변경
- **이벤트 핸들링**:
  - `completed`: Job 완료 로그
  - `failed`: Job 실패 로그
  - `error`: Redis 연결 에러
  - `ready`: Redis 연결 성공
- **연관 파일**:
  - `metaAdLibrary.js` - 실제 스크래핑 로직
  - `dbService.js` - Job 상태 업데이트

---

### 💾 서비스 (Services)

#### [dbService.js](backend/src/services/dbService.js)
- **위치**: `backend/src/services/dbService.js`
- **주요 기능**:
  - Supabase DB 작업을 위한 헬퍼 함수
  - Job 상태 관리
  - 광고 및 미디어 저장
- **주요 함수**:
  - `createJob(userId, jobId, searchType, searchQuery)` - Job 레코드 생성
  - `updateJobStatus(jobId, status, totalAds)` - Job 상태 업데이트
  - `saveAd(userId, adData, platform)` - 광고 저장
  - `saveMedia(adId, mediaData)` - 미디어 저장
- **연관 파일**:
  - `scrapeRoutes.js` - Job 생성
  - `scrapeQueue.js` - Job 상태 업데이트
  - `supabase.js` - Supabase 클라이언트

---

#### [cloudinaryService.js](backend/src/services/cloudinaryService.js)
- **위치**: `backend/src/services/cloudinaryService.js`
- **주요 기능**:
  - Cloudinary API를 통한 이미지/동영상 업로드
  - 미디어 URL 다운로드 및 업로드
  - 업로드 실패 시 재시도 로직
- **주요 함수**:
  - `uploadImage(imageUrl, options)` - 이미지 업로드
  - `uploadVideo(videoUrl, options)` - 동영상 업로드
  - `uploadMedia(mediaUrl, mediaType, options)` - 자동 타입 감지 업로드
  - `deleteMedia(publicId, resourceType)` - 미디어 삭제
- **업로드 프로세스**:
  1. URL에서 직접 업로드 시도 (Cloudinary URL fetch)
  2. 실패 시 파일 다운로드 후 재시도
  3. 임시 파일 삭제
- **옵션**:
  - `folder` - Cloudinary 폴더 경로
  - `adId` - 광고 ID (파일명에 포함)
  - `position` - 미디어 순서
- **반환 데이터**:
  - `url` - Cloudinary URL
  - `publicId` - Cloudinary Public ID
  - `width`, `height` - 해상도
  - `format` - 파일 포맷
  - `bytes` - 파일 크기
- **연관 파일**:
  - `metaAdLibrary.js` - 스크래핑 시 미디어 업로드

---

#### [ocrService.js](backend/src/services/ocrService.js)
- **위치**: `backend/src/services/ocrService.js`
- **주요 기능**:
  - Tesseract.js를 사용한 OCR (광학 문자 인식)
  - 이미지에서 한글/영문 텍스트 추출
  - 키워드 자동 추출
- **주요 함수**:
  - `extractTextFromImage(imageUrl, options)` - 단일 이미지 OCR
  - `extractTextFromImages(imageUrls, options)` - 여러 이미지 일괄 OCR
  - `extractKeywords(ocrText)` - OCR 텍스트에서 키워드 추출
- **OCR 옵션**:
  - `lang` - 언어 설정 (기본: 'kor+eng' - 한국어+영어)
  - `verbose` - 상세 로그 출력
- **반환 데이터**:
  - `text` - 추출된 텍스트
  - `confidence` - 신뢰도 (0-100)
  - `lang` - 감지된 언어
- **키워드 추출**:
  - 가격 패턴 (예: "50%", "10,000원")
  - 짧은 문구 (5-30자)
- **현재 상태**: 속도 개선을 위해 비활성화 (metaAdLibrary.js에서)
- **연관 파일**:
  - `metaAdLibrary.js` - 스크래핑 시 OCR 사용 (현재 비활성화)

---

#### [imageService.js](backend/src/services/imageService.js)
- **위치**: `backend/src/services/imageService.js`
- **주요 기능**:
  - 이미지 다운로드 및 처리
  - 이미지 메타데이터 추출
- **연관 파일**:
  - `cloudinaryService.js` - 이미지 업로드
  - `metaAdLibrary.js` - 이미지 처리

---

### 🔐 미들웨어 (Middleware)

#### [auth.js](backend/src/middleware/auth.js)
- **위치**: `backend/src/middleware/auth.js`
- **주요 기능**:
  - API 요청 인증 미들웨어
  - JWT 토큰 검증 (Supabase Auth)
  - 개발 환경 인증 우회
- **핵심 동작**:
  1. Authorization 헤더에서 Bearer 토큰 추출
  2. Supabase Auth로 토큰 검증
  3. 유효한 경우 `req.user`에 사용자 정보 설정
  4. 무효한 경우 401 에러 반환
- **개발 모드**:
  - `DISABLE_AUTH=true` 환경변수 설정 시 인증 우회
  - 테스트용 고정 사용자 ID 사용
- **연관 파일**:
  - `scrapeRoutes.js` - `requireAuth` 미들웨어 사용
  - `jobRoutes.js` - `requireAuth` 미들웨어 사용

---

### ⚙️ 설정 (Config)

#### [supabase.js](backend/src/config/supabase.js) (Backend)
- **위치**: `backend/src/config/supabase.js`
- **주요 기능**:
  - Supabase 클라이언트 초기화 (서버용)
  - Service Role Key 사용 (관리자 권한)
- **환경변수**:
  - `SUPABASE_URL` - Supabase 프로젝트 URL
  - `SUPABASE_SERVICE_ROLE_KEY` - Service Role Key (비공개)
- **연관 파일**:
  - 모든 백엔드 파일에서 import

---

## 🗂️ 데이터베이스 구조 (Supabase)

### 테이블 목록

1. **ad_archives** (광고 메타데이터)
   - 광고 기본 정보 (광고주, 텍스트, 링크, 날짜)
   - `ad_archive_id` (Meta 고유 ID)로 중복 방지

2. **ad_media** (광고 미디어)
   - 이미지/동영상 URL
   - Cloudinary 업로드 정보
   - OCR 추출 텍스트
   - 메타데이터 (크기, 포맷, 비율)

3. **scrape_jobs** (스크래핑 Job)
   - Job ID, 상태, 진행률
   - 검색 타입, 검색어
   - 생성/시작/완료 시간

4. **user_search_history** (검색 히스토리)
   - 사용자별 검색 기록
   - 검색 타입 (keyword / advertiser)
   - 총 광고 개수
   - 마지막 검색 시간

---

## 🔄 데이터 흐름

### 1. 스크래핑 요청 플로우
```
SearchBar (Frontend)
  → scrapeByKeyword() (API Service)
  → POST /api/scrape/keyword (Backend Route)
  → addScrapeJob() (Bull Queue)
  → scrapeMetaAds() (Scraper)
  → Supabase DB 저장
  → Job 완료
  → JobTracker (Frontend) 상태 표시
  → AdGrid 새로고침
```

### 2. 광고 조회 플로우
```
index.jsx (Frontend)
  → getAds() (API Service)
  → Supabase 직접 쿼리
  → AdGrid 표시
  → 클릭 시 AdDetailModal 오픈
```

### 3. 검색 히스토리 플로우
```
SavedSearchesSidebar (Frontend)
  → getSearchHistory() (API Service)
  → Supabase 쿼리
  → 검색 항목 클릭
  → index.jsx 필터 적용
  → getAds() 재조회
```

---

## 🛠️ 유틸리티 스크립트 (Backend Root)

백엔드 루트 디렉토리에는 다양한 테스트 및 유틸리티 스크립트가 있습니다:

- **테스트 스크립트**:
  - `test-playwright.js` - Playwright 브라우저 테스트
  - `test-graphql.js` - GraphQL API 테스트
  - `test-korea-ads.js` - 한국 광고 스크래핑 테스트
  - `test-scraper-headless.js` - 헤드리스 모드 테스트

- **데이터 검증**:
  - `check_ad.js` - 광고 데이터 검증
  - `check_duplicate_images.js` - 중복 이미지 체크
  - `check_empty_text.js` - 빈 텍스트 체크
  - `check_constraints.js` - DB 제약조건 확인

- **데이터 관리**:
  - `cleanup-duplicate-media.js` - 중복 미디어 정리
  - `upload-media-to-cloudinary.js` - Cloudinary 일괄 업로드
  - `find_duplicates.js` - 중복 광고 찾기

- **마이그레이션**:
  - `run-migration.js` - DB 마이그레이션 실행
  - `apply-migration-004.js` - 특정 마이그레이션 적용

---

## 📝 주요 기술 스택

### Frontend
- React 18
- Chakra UI (UI 컴포넌트)
- Axios (HTTP 클라이언트)
- Supabase JS (DB 직접 접근)

### Backend
- Node.js + Express
- Playwright (웹 스크래핑)
- Bull (Job Queue, Redis 기반)
- Supabase (PostgreSQL)
- Cloudinary (미디어 호스팅)
- Tesseract.js (OCR, 선택적)

### Infrastructure
- Redis (Job Queue)
- PostgreSQL (Supabase)
- Cloudinary (미디어 스토리지)

---

## 🚀 개선 포인트 및 참고사항

1. **스크래핑 속도 최적화**:
   - Cloudinary 업로드는 선택적 (`uploadToCloudinary` 플래그)
   - OCR은 현재 비활성화 (속도 개선)

2. **중복 방지**:
   - `ad_archive_id` 기반 upsert로 중복 광고 방지
   - 미디어도 `ad_id + position` 조합으로 중복 방지

3. **에러 처리**:
   - 저장 실패 시 3회 재시도
   - 실패한 광고 목록 로그 출력

4. **반응형 디자인**:
   - 모바일: SearchBar 아래 인라인 히스토리
   - 데스크톱: 고정 사이드바 히스토리

5. **실시간 업데이트**:
   - JobTracker로 2초마다 진행 상황 폴링
   - 완료 시 자동으로 광고 목록 새로고침
